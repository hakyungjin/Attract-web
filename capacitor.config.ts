import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.attract.app',
  appName: 'Attract',
  webDir: 'out',
  android: {
    backgroundColor: '#FFFFFF'
  },
  ios: {
    backgroundColor: '#FFFFFF'
  }
};

export default config;
